============
Installation
============

At the command line::

    $ easy_install django-time-metrics

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-time-metrics
    $ pip install django-time-metrics
